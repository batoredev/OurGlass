/**
 * Placeholder for Phase 0. Phase 2 (docs/PHASES.md) replaces this with the
 * ~50 hand-labelled utterances from the spec's own examples (Barkha
 * article, Hult poster, the two Aruns, the 5 PM conflict, "she had a family
 * emergency") run against recorded fixtures — no live model calls, so this
 * lane stays free and deterministic in CI.
 */
import { describe, expect, it } from "vitest";

describe("eval harness scaffold", () => {
  it("is wired and ready for Phase 2 fixtures", () => {
    expect(true).toBe(true);
  });
});
