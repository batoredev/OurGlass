/**
 * @ourglass/db
 *
 * Placeholder for Phase 0. Phase 1 (docs/PHASES.md) adds migrations and the
 * bitemporal schema (commitments, people, organizations, projects, events,
 * reminders, relationships, workflows, permissions, messages, documents,
 * memories, action_log — see docs/EXECUTION-PLAN.md's data model section).
 * Sole owner in team missions: database-data-engineer.
 */
export const DB_PACKAGE_PLACEHOLDER = true as const;
